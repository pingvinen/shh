#!/usr/bin/env node
var Config = require('./../lib/Config.js');
var config = new Config();

config.mongoDatabaseName = 'shh';
config.sessionSecret = '*** set this to something ***';

module.exports = config;

